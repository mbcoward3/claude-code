/* plan-ui: placeholder for vendored @tailwindcss/browser.
   Replaced at build time by scripts/vendor-assets.sh with the real JIT bundle.
   Until vendored, this no-op lets the binary build and run without a CDN. */
console.warn("[plan-ui] Tailwind runtime not vendored yet; run scripts/vendor-assets.sh");
